m_list = [1, 2, 3, 4, 5]
m_tuple = (1, 2, 3, 4, 5)
m_str = "abcdef"
m_set = {1, 2, 3, 4, 5}
m_dict = {"key1": 1, "key2": 2, "key3": 3, "key4": 4}

# len 元素个数

# min 最小值

# max 最大值

# 类型转换：容器转列表
result = list(m_tuple)
print(f"转换后的结果为：{result}, 类型为：{type(result)}")

# sorted(容器，reverse=False) 排序，结果会变成列表

